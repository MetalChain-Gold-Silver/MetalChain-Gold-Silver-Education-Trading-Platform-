# Setup Notes

## Current State

This repository is intentionally scaffolded before full app code is added.

That means contributors can help with:

- product planning
- docs
- architecture
- UX exploration
- initial app bootstrapping

## Suggested Next Setup Tasks

1. Bootstrap `frontend` with Next.js and TypeScript
2. Bootstrap `backend` with Node.js and TypeScript
3. Add formatting and linting
4. Add tests and CI
5. Add a clean boundary for Soroban integration in `smart-contract`

## Suggested Standards

- TypeScript across frontend and backend
- ESLint and Prettier
- PostgreSQL for persistent data
- clear adapter boundary for Stellar and Soroban integrations
